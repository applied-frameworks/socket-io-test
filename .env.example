PORT=3000
JWT_SECRET=your-super-secret-jwt-key-change-this-in-production
NODE_ENV=development
CLIENT_URL=http://localhost:8000

# Database Configuration
# Choose ONE of the following DATABASE_URL options:

# Option 1: SQLite (Quick local development - not recommended for production parity)
# DATABASE_URL="file:./dev.db"

# Option 2: PostgreSQL via Docker (Recommended - production parity)
# Start Docker: docker compose up -d
# DATABASE_URL="postgresql://postgres:postgres@localhost:5432/canvas_dev?schema=public"

# Option 3: PostgreSQL (Remote/Hosted)
# DATABASE_URL="postgresql://user:password@host:5432/dbname?schema=public"
